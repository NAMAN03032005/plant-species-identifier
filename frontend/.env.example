# LeafScan Frontend Environment Variables Configuration
# Set VITE_API_URL for production deployment if Flask API is hosted on a separate origin.
# Leave blank or omit for local Vite dev proxy (http://localhost:5173 -> http://127.0.0.1:5000)
VITE_API_URL=http://localhost:5000
