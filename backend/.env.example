# LeafScan Flask Backend Environment Configuration (Render Production Template)
# PORT is automatically injected by Render (defaults to 5000 locally)
PORT=5000

# Specify production React frontend URL to allow cross-origin requests
# Example: FRONTEND_ORIGIN=https://leafscan-frontend.onrender.com
FRONTEND_ORIGIN=http://localhost:5173
